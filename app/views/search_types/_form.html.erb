<%= form_with(model: search_type) do |form| %>
  <% if search_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_type.errors.count, "error") %> prohibited this search_type from being saved:</h2>

      <ul>
        <% search_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :search_type_id, style: "display: block" %>
    <%= form.number_field :search_type_id %>
  </div>

  <div>
    <%= form.label :search_type_content, style: "display: block" %>
    <%= form.text_field :search_type_content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
